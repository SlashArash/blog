import { MetadataRoute } from 'next';

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: '/keystatic/', // Hide the admin panel from search results
    },
    sitemap: 'https://kadkhodaei.ir/sitemap.xml',
  };
}