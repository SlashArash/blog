type SiteMetadata = {
  author: string
  description: string
  siteUrl: string
  social: {
    linkedin: string
    facebook: string
    nickname: string
  }
  title: string
}

export default SiteMetadata
